NOTE (curated by Task 1 gold-mining harness, review-round-1 Fix 2): the
commit/repo/file/line/literal/token below are 100% real -- captured with
`git log -L26,31:polaris.shopify.com/src/components/Button/Button.module.scss
-1 6e958b1c597fcc98dadc2d24d8e7c29a397e20c5` against the real Shopify/polaris
repo (byte-real, no hand-editing anywhere in the hunk below). A line-range
`git log -L` was used instead of a whole-file `git show <sha> -- <file>`
because this commit's SAME file also has one earlier, unrelated hunk (a
`display: inline-block;` / `color: inherit;` layout tweak, no colour
content) that would otherwise make the fixture non-single-hunk; `git log -L`
is a first-class git feature (line-history tracking), not a hand edit -- it
produces the same kind of byte-real, tool-generated diff output as `git
show`, just scoped to a line range. This method was needed only after an
exhaustive pickaxe scan across 16+ non-Lyse OSS design-system repos found no
other real commit with an exact-value-preserving hex->var(--x) swap that was
ALSO naturally single-hunk for the whole file. See .tokendef for the full
value-equality proof and task-1-report.md's "Fix pass (review round 1)"
section for the search trail.

commit 6e958b1c597fcc98dadc2d24d8e7c29a397e20c5
Author: Marten Bjork <marten.bjork@shopify.com>
Date:   Thu Apr 28 15:08:27 2022 +0200

    Use next/link for components filter (#5649)
    
    * Use next/link for components filter
    
    * Fix margin
    
    * Update polaris.shopify.com/src/components/Button/Button.tsx
    
    Co-authored-by: Aaron Casanova <32409546+aaronccasanova@users.noreply.github.com>
    
    * Update polaris.shopify.com/src/components/Button/Button.tsx
    
    Co-authored-by: Aaron Casanova <32409546+aaronccasanova@users.noreply.github.com>
    
    * Add className utility function
    
    * Change order of types
    
    Co-authored-by: Aaron Casanova <32409546+aaronccasanova@users.noreply.github.com>

diff --git a/polaris.shopify.com/src/components/Button/Button.module.scss b/polaris.shopify.com/src/components/Button/Button.module.scss
--- a/polaris.shopify.com/src/components/Button/Button.module.scss
+++ b/polaris.shopify.com/src/components/Button/Button.module.scss
@@ -24,5 +26,6 @@
   &[aria-pressed="true"],
-  &[aria-selected="true"] {
-    background: #008060;
+  &[aria-selected="true"],
+  &[aria-current="true"] {
+    background: var(--primary);
     color: white;
   }
