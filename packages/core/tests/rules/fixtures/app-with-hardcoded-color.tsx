/**
 * Fixture: counter-example — a normal app file with a hardcoded color.
 * Hardcoded hex values here are drift (the developer should use a token).
 * Confidence should remain "high".
 */
export function Banner() {
  return <div style={{ color: "#3b82f6" }}>Hello</div>;
}
