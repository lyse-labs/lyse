/**
 * Fixture: canonical design-token definition file.
 * Hardcoded hex values here are EXPECTED — they define the tokens themselves.
 * Confidence should be lowered to "medium" so `lyse fix` does not auto-rewrite them.
 */
export const colors = {
  primary: "#3b82f6",
  secondary: "#f59e0b",
  danger: "#ef4444",
  success: "#22c55e",
  neutral: "#64748b",
};
