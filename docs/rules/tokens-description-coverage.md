# `tokens/description-coverage`

> **Axis:** Tokens · **Severity:** info · **Auto-fixable:** no

Stub — full doc pending. Measures the percentage of design tokens that include a `$description` field (DTCG spec).

## Why
Tokens without descriptions are harder for AI agents (and humans) to consume correctly. The DTCG spec recommends `$description` for every token.

