# `ai-surface/component-manifest-json`

> **Axis:** AI surface · **Severity:** info · **Auto-fixable:** no

Stub — full doc pending. Detects whether the design system ships a `components.json` manifest listing components and their source files.

## Why
Component manifests let MCP servers and other AI agents resolve component lookups at ~5× lower cost than reading source files.

